# Commit current changes

Commit the current changes with a good commit message

## Instructions

- Identify the ticket number from branch name
- Run `git status` to check for uncommitted changes
- Run `git add necessary_files` to add files that needs to commit
- Run `git diff --cached -w -b` to see what have changed
- Run `git commit -m 'ticket number: commit message'

